// BackendTrashResource automatically generated by SDKgen please do not edit this file manually
// @see https://sdkgen.app

package backend

import (
	"bytes"
	"encoding/json"
	"errors"
	"github.com/apioo/sdkgen-go"
	"io"
	"net/http"
	"net/url"
)

type BackendTrashResource struct {
	url    string
	client *http.Client
}

// BackendActionTrashGetTypes
func (resource BackendTrashResource) BackendActionTrashGetTypes() (TrashTypes, error) {
	url, err := url.Parse(resource.url)
	if err != nil {
		return TrashTypes{}, errors.New("could not parse url")
	}

	req, err := http.NewRequest("GET", url.String(), nil)
	if err != nil {
		return TrashTypes{}, errors.New("could not create request")
	}

	resp, err := resource.client.Do(req)
	if err != nil {
		return TrashTypes{}, errors.New("could not send request")
	}

	defer resp.Body.Close()

	respBody, err := io.ReadAll(resp.Body)
	if err != nil {
		return TrashTypes{}, errors.New("could not read response body")
	}

	var response TrashTypes

	err = json.Unmarshal(respBody, &response)
	if err != nil {
		return TrashTypes{}, errors.New("could not unmarshal JSON response")
	}

	return response, nil
}

func NewBackendTrashResource(resource *sdkgen.Resource) *BackendTrashResource {
	return &BackendTrashResource{
		url:    resource.BaseUrl + "/backend/trash",
		client: resource.HttpClient,
	}
}
