// BackendActionFormResource automatically generated by SDKgen please do not edit this file manually
// @see https://sdkgen.app

package backend

import (
	"bytes"

	"errors"
	"github.com/apioo/sdkgen-go"

	"net/http"
	"net/url"
)

type BackendActionFormResource struct {
	url    string
	client *http.Client
}

// BackendActionActionGetForm
func (resource BackendActionFormResource) BackendActionActionGetForm(query FormQuery) (FormContainer, error) {
	url, err := url.Parse(resource.url)
	if err != nil {
		return FormContainer{}, errors.New("could not parse url")
	}

	rawJson, err := json.Marshal(query)
	if err != nil {
		return FormContainer{}, errors.New("could not marshall query")
	}

	parameters := url.Query()
	err = json.Unmarshal(rawJson, &parameters)
	url.RawQuery = parameters.Encode()

	req, err := http.NewRequest("GET", url.String(), nil)
	if err != nil {
		return FormContainer{}, errors.New("could not create request")
	}

	resp, err := resource.client.Do(req)
	if err != nil {
		return FormContainer{}, errors.New("could not send request")
	}

	defer resp.Body.Close()

	respBody, err := io.ReadAll(resp.Body)
	if err != nil {
		return FormContainer{}, errors.New("could not read response body")
	}

	var response FormContainer

	err = json.Unmarshal(respBody, &response)
	if err != nil {
		return FormContainer{}, errors.New("could not unmarshal JSON response")
	}

	return response, nil
}

func NewBackendActionFormResource(resource *sdkgen.Resource) *BackendActionFormResource {
	return &BackendActionFormResource{
		url:    resource.BaseUrl + "/backend/action/form",
		client: resource.HttpClient,
	}
}
